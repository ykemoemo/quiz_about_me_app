- content_for(:html_title) { @quiz.creator_name + "王 - 〇〇王！！" }
h1 結果
br

h3 #{@challenger.score} / #{@questions_count} 問正解
br

= render "questions/check_the_answer", questions: @questions, quiz: @quiz

= render "challengers/ranking", challengers_ranking: @challengers_ranking

= render "challengers/share_link", quiz: @quiz, questions_count: @questions_count, challenger: @challenger

= link_to '問題を作成する', root_path, class: 'radius btn btn-secondary'
