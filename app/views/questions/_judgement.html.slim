#judgement_ajax
  .question-box
    .box-title
      p = question.body
    - question.choices.each do |choice|
      = button_to choice.body, quiz_challenger_question_choice_judgements_path(quiz.id, challenger.id, question.id, choice.id), class: 'choice-radius btn btn-outline-secondary', remote: true
  br
  br
